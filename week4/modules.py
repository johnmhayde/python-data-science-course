# modules are files that contain python code
# can contains classes, functions, other useable code
# use keyword import to include module

import os

# get working directory
print(os.getcwd())

# list all files in directory
print(os.listdir())
